package com.algorithmlx.algolib.config;

public class AlgoLibConfig {
    @Config(config = "algoLib", key = "Allow example armor", comment = "Standard - false (for developers)")
    public static boolean allowExampleArmor = false;
}
